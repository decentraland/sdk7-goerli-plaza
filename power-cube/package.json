{
  "name": "power-cube",
  "description": "Power Cube: Decentraland scene project",
  "version": "1.0.0",
  "dependencies": {
    "@dcl-sdk/utils": "^1.0.2",
    "@dcl/sdk": "next"
  },
  "scripts": {
    "build": "sdk-commands build --skip-install",
    "deploy": "sdk-commands deploy",
    "start": "sdk-commands start"
  },
  "bundleDependencies": [
    "@dcl-sdk/utils"
  ]
}
