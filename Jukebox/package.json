{
  "name": "Jukebox",
  "description": "ECS7 ",
  "version": "1.0.0",
  "bundleDependencies": [
    "@dcl-sdk/utils"
  ],
  "dependencies": {
    "@dcl-sdk/utils": "^1.0.2",
    "@dcl/sdk": "next"
  },
  "scripts": {
    "build": "sdk-commands build --skip-install",
    "deploy": "sdk-commands deploy",
    "start": "sdk-commands start"
  }
}
